{
    "build": {
      "env": {
        "DATABASE_URL": "@database_url"
      }
    },
    "functions": {
      "api/**.ts": {
        "runtime": "nodejs18.x"
      }
    },
    "buildCommand": "prisma generate && next build"
  }